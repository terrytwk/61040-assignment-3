{
  "name": "60140-assignment-3",
  "version": "1.0.0",
  "description": "A demo application showing how to use Google's Gemini API to take customer orders",
  "main": "dist/customerordering-tests.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/customerordering-tests.js",
    "dev": "ts-node --transpile-only customerordering-tests.ts",
    "test": "npm run build && node dist/customerordering-tests.js",
    "manual": "npm run build && node -e \"require('./dist/customerordering-tests.js').testManualScheduling()\"",
    "llm": "npm run build && node -e \"require('./dist/customerordering-tests.js').testLLMScheduling()\"",
    "mixed": "npm run build && node -e \"require('./dist/customerordering-tests.js').testMixedScheduling()\""
  },
  "keywords": [
    "gemini",
    "ai",
    "customer ordering",
    "student",
    "demo",
    "typescript"
  ],
  "author": "Student Demo",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
